- hosts: webservers
  become: true
  tasks:
    - name: Создание пользователя deploy
      user:
        name: deploy
        shell: /bin/bash
        create_home: yes
        system: yes
  
    - name: Создание директории развертывания
      file:
        path: /home/deploy/my-app
        state: directory
        owner: deploy
        group: deploy
        mode: 0755

    - name: Копирование docker-compose.yml
      copy:
        src: ../docker-compose.yml
        dest: /home/deploy/my-app/docker-compose.yml
        owner: deploy
        group: deploy
        mode: 0644
 
    - name: ✏️ Debug domain
      debug:
        msg: "Domain is: {{ ansible_domain }}"
        
    - name: Создание .env файла
      copy:
        content: |
          DB_NAME=wordpress
          DB_USER=root
          DB_PASSWORD=my-secret-pw
          DB_HOST=mariadb
          HOST_DOMAIN=w114w11w97.zapto.org
        dest: /home/deploy/my-app/.env
        owner: deploy
        group: deploy
        mode: 0644

    - name: Очистить неиспользуемые ресурсы Docker
      shell: docker system prune -af

    - name: 💿 Копирование tar-архивов Docker-образов
      copy:
        src: ../{{ item }}
        dest: /home/deploy/my-app/{{ item }}
        owner: deploy
        group: deploy
        mode: 0644
      loop:
        - mariadb.tar
        - nginx.tar


    - name: 🐳 Загрузка Docker-образов
      command: docker load -i /home/deploy/my-app/{{ item }}
      args:
        chdir: /home/deploy/my-app
      loop:
        - mariadb.tar
        - nginx.tar
      ignore_errors: true # Игнорировать ошибки, если образы уже загружены

    # - name: Остановить и удалить все контейнеры и тома docker-compose
    #   shell: docker-compose down -v
    #   args:
    #     chdir: /home/deploy/my-app

    # try --force-recreate
    - name: 🐳 Deploy with rollback on failure
      block:
        - name: Deploy services
          command: docker-compose up -d 
          args:
            chdir: /home/deploy/my-app
